"""Bundled MCP servers that ship with Return Architecture.

Each is runnable via:
    python -m return_architecture.mcp_servers.<name>

Configure in an agent's config.toml under [mcp.servers.<name>].
"""
